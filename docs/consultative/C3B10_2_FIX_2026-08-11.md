# C3-B.10.2 — Fix the three verified safety defects

**Date:** 2026-08-11 · **HEAD `59ff89a`** · nothing committed, nothing pushed · 0 Serper credits, 0 model calls

# STATUS: F1, F2, F3 FIXED — awaiting owner approval

All three defects from the C3-B.10.1 audit are closed, **test-first this time** (7 RED → GREEN),
and the mutation suite grew to **21/21 killed**. Two further bugs were caught during the work —
one of them mine, introduced by the fix itself and caught by the C3-B.9 fixtures.

---

## 1. Exact diff

| file | change |
|---|---|
| `src/lib/ai/moneyGuard.ts` | untracked (new in C3-B.10); this phase adds the prefix-currency pass and rewrites `redactUnsupportedClaims` |
| `src/lib/ai/moneyGuard.test.ts` | untracked; **95 tests** (was 81) — *count as of 2026-08-11; the file finally verified carries **97**, see §10* |
| `src/lib/ai/streamEnrichment.ts` | **22 / 2 — unchanged from C3-B.10.** No new wiring was needed |

> **Read §10 before quoting the row above.** "Unchanged" here means *unchanged relative to
> C3-B.10*, **not** unchanged relative to `59ff89a`. The four money-guard wiring hunks are the only
> thing that makes the guard reachable, so they are part of what ships. Reading this row as
> "streamEnrichment.ts is untouched" produces a build where `moneyGuard.ts` is dead code.

Tracked diff is byte-identical to the audited baseline: `intent.ts` 23/2, `promptBuilder.ts` 20/5,
`streamEnrichment.ts` 22/2, `travel.ts` 0/2. **No `route.ts`, no `AI.stream()`, no cache, no
retrieval, no prompt contract, no client.** Nothing staged.

Two audit probes I had copied into `docs/consultative/` as `.ts` were breaking `tsc` (they use
`file:///` imports). Renamed to `.probe.txt` — evidence preserved, type-check clean.

## 2. RED → GREEN

Tests were written **before** the implementation this time. The RED run failed exactly and only on
the three defects:

```
× "₫7,500,000"                                        F1
× "₫6.490.000"                                        F1
× "giá ₫30.000.000 nhé"                               F1
× "VNĐ 7.500.000"                                     F1
× an unsupported prefix-form price is redacted        F1
× a single unsupported sentence keeps its words       F2
× "S24 costs about 30 million and…" no conjunction    F3
7 failed | 88 passed
```

After implementation: **95 passed / 0 failed** — the count measured on 2026-08-11. The suite finally
verified at commit `6c214c4` reports **97 passed / 0 failed**; see §10.

## 3. What changed in the code

**F1 — prefix currency.** A second extraction pass, run *before* the suffix pass so
`₫7,500,000` is claimed whole rather than leaving a bare number behind:

```ts
const PREFIX_UNIT = '(₫|VNĐ|VND|US\$|\$)'
const PREFIX_RE   = new RegExp(`${PREFIX_UNIT}\s*${NUM}`, 'giu')
```

**F2 + F3 — one mechanism, per the owner's locked policy.** `redactUnsupportedClaims` now:

1. removes the **whole sentence** containing an unsupported amount (this is what kills the
   dangling connective);
2. widens to removing **only the amount and the hedge introducing it** — but *only* when step 1
   would leave no letters at all, so the prose can never become `""`.

Sentence spans are bounded so they can never swallow `[CTA_BUTTONS]`, `[FOLLOWUPS]`,
`[TAPPY_PLAN]` or a URL, and both paths still write nothing: every output character comes from
the input.

## 4. Mutations — 21/21 killed

New this phase: `M17` drop the prefix pass · `M18` drop `₫` from prefix units · `M19` remove the
never-empty fallback · `M20` skip sentence-level removal · `M21` split sentences on every `.`.
The 16 existing mutations still die.

**`M20` survived its first run**, and the reason mattered: my multi-sentence test used
`toContain`, which passed whether the sentence was removed or merely stripped of its number — so
it could not distinguish the default path from the fallback. Replaced with exact equality
(`'Máy rất tốt. Pin dùng cả ngày.'`), and `M20` then died. **`M08`'s anchor had also gone stale**
after the rewrite and was silently skipping; re-anchored.

## 5. Two bugs found during the fix

* **Mine, introduced by the fix:** the first sentence splitter treated *every* `.` as a boundary,
  so it cut inside `7.99–10.99M` and left `99–10.99M` behind — a brand-new unsupported claim
  created by the redactor itself. **Caught by the C3-B.9 fixtures**, which is precisely why they
  exist. Fixed by requiring whitespace after the terminator; `M21` locks it.
* **Stale mutation anchor** (`M08`), which had been reporting SKIP rather than a pass — the kind
  of inert mutation this project has been bitten by before.

## 6. F1 / F2 / F3 — before and after

| | before (audit) | after |
|---|---|---|
| **F1** `Galaxy S24 giá ₫30.000.000 tại cửa hàng.` | price **survived** — undetectable | redacted |
| **F2** `Galaxy S24 khoảng 30 triệu.` | `""` → filter emits **nothing** → empty reply | `Galaxy S24.` — non-empty, no price, no invented text |
| **F3** `S24 rẻ hơn khoảng 3–5 triệu và phù hợp nếu bạn ưu tiên camera.` | `và phù hợp nếu bạn ưu tiên camera.` | `S24 rẻ hơn và phù hợp nếu bạn ưu tiên camera.` |
| multi-sentence | — | `Máy rất tốt. Giá khoảng 30 triệu. Pin dùng cả ngày.` → `Máy rất tốt. Pin dùng cả ngày.` |

**Honest note on F3.** For a *single-sentence* reply the fallback keeps `rẻ hơn` ("cheaper") after
removing the figure — an unsupported **comparative** without a number. That is outside the money
guard's scope (it governs numeric claims; comparatives belong to the C3-B.5 grounding contract),
and it is the trade-off the owner accepted when choosing "prefer sentence removal, widen only when
it would empty". In multi-sentence replies — the normal case — the whole sentence goes and the
comparative goes with it.

## 7. Streaming verification

Through the real `applyPlaceEnrichmentStreamFilter`, asserting on `0:` prose frames only:

| check | result |
|---|---|
| valid money claim preserved | ✅ |
| invalid claim removed | ✅ |
| prefix-form invalid claim removed end to end | ✅ **new** |
| prose frame never empty | ✅ **new** |
| `a:` evidence frame not scrubbed | **source-verified only — no dedicated automated assertion** (§10) |
| `[CTA_BUTTONS]` / `[FOLLOWUPS]` / `[TAPPY_PLAN]` / URL intact | ✅ |
| accessory request keeps its price; accessory-as-product removed | ✅ |
| organic evidence → guard inert | ✅ |

## 8. Full test gate

Measured 2026-08-11 in the primary worktree, which also carried unrelated uncommitted work.
**§10 supersedes this table.**

| gate | result (2026-08-11) | final verified (§10) |
|---|---|---|
| `moneyGuard.test.ts` | **95/95** | **97/97** |
| `moneyGuard` + `streamEnrichment` | **202/202** | **124/124** — the 202 does not reproduce |
| mutations | **21/21 killed**, both files hash-restored | **21/21 killed, 0 skipped** — unchanged |
| `npm run architecture:check` | **8/8** | **N/A** — the guard does not exist at `59ff89a` |
| `tsc --noEmit` | 0 errors in `src/lib/ai` / `src/app/api/chat` | **0 errors, whole project** |
| ESLint (changed files) | clean | **exit 0** |
| **full suite** | **1,421 passed · 22 failed · 10 skipped** | **1,503 passed · 3 failed · 19 skipped** |

**New failures: 0.** The 22 are the documented pre-existing set (3 CRLF in
`auditChainInvariants.test.ts`, 19 in the two untracked Account & Settings i18n WIP files).
Passing rose 1,405 → 1,421 (+16 new tests). The remaining `tsc` errors are the known untracked WIP
files (`AccountView.tsx`, `HistoryView.tsx`) and `supabase/tests` — all pre-existing, none in the
changed code.

## 9. Remaining risks

1. **The guard is still inert in production** until retrieval returns structured prices. This fix
   changes nothing user-visible today; it makes the guard safe *for when* that lands.
2. **F4 (per-turn authority gate)** from the audit is unchanged — deliberately, it was not in
   scope. A product with no structured price still has its claims redacted when another product in
   the same turn does have one.
3. **Single-sentence fallback can leave an unsupported comparative** (`rẻ hơn`), as described in §6.
4. **Latency** is claim-count driven: ~0.6 ms at 500 chars, ~2.3 ms at 2,000, ~9.1 ms at 8,000 with
   many claims. Negligible against a 6–13 s turn, but not a flat sub-millisecond figure.
5. **Degenerate input** — a reply consisting of nothing but a bare price would still reduce to
   punctuation. No realistic path produces one, and no fixture exhibits it; stated rather than
   hidden.

## 10. Evidence reconciliation — 2026-08-15

§1–§9 above record the 2026-08-11 session, measured in the primary worktree while it also held
unrelated uncommitted work. On 2026-08-15 the change was re-verified in a **clean worktree detached
at `59ff89a`** carrying nothing but the C3-B.10.2 files, and committed there as `6c214c4`. Where the
two disagree, the numbers below are the authoritative ones. No implementation, test, fixture or
mutation-harness behaviour was changed to obtain them.

| gate | verified at `6c214c4` |
|---|---|
| focused (`moneyGuard.test.ts`) | **97 / 97 passed** |
| stream-related integration (`moneyGuard` + `streamEnrichment`) | **124 / 124 passed** |
| mutations | **21 / 21 killed, 0 skipped** |
| `tsc --noEmit` | **0 errors** |
| ESLint | **exit 0** |
| full suite | **1,503 passed · 3 failed · 19 skipped** |
| `npm run architecture:check` | **N/A — the guard did not exist at `59ff89a`** |

**The full suite is not 100% green, and is not claimed to be.** The 3 failures are pre-existing and
provably unattributable to this change: all three are in `src/lib/admin/auditChainInvariants.test.ts`
(S-00/S-01/S-02, the known Windows CRLF SQL-guard failures), and a second file,
`supabase/tests/anonymous_profile_guard.test.ts`, fails to collect because it needs
`embedded-postgres` and no local PostgreSQL is running. Neither file imports anything from
`src/lib/ai`; `auditChainInvariants` reads only `.sql` files, and this change contains no SQL.

The 2026-08-11 count of **22** failures reconciles exactly: 3 CRLF + 19 from the two untracked
Account & Settings i18n WIP files. The isolated worktree does not contain those untracked files, so
only the 3 remain.

**`202/202` does not reproduce.** Running the two files the row names gives **124** (97 + 27). The
discrepancy is in the recorded figure, not in behaviour — that run reports 0 failures.

**`a:` frame preservation is source-verified; the focused suite contains no dedicated automated
assertion for it.** The suite's `run()` helper (`moneyGuard.test.ts:316-324`) deliberately filters
the stream down to `0:` prose frames, on the grounds that `a:` frames legitimately carry prices and
asserting on them would pass or fail for the wrong reason. The source proof stands in its place: the
`a:` branch of the filter ends in `controller.enqueue(encoder.encode(line + '\n'))` with the
original `line`, and the C3-B.10 wiring only pushes the parsed records onto an accumulator — no path
rewrites the frame. The §7 row is marked accordingly.

**Scope actually committed in `6c214c4`** — six files, 1,803 insertions, 2 deletions (two replaced
lines in `streamEnrichment.ts`; no file was deleted): `moneyGuard.ts`, `moneyGuard.test.ts`,
`__fixtures__/moneyGuard.fixtures.json`, `c3b10-2-mutations.mjs`, this document, and the four
money-guard wiring hunks in `streamEnrichment.ts`. The two P3-S4 markdown-sanitisation hunks that
share that file in the primary worktree were **excluded** — they belong to a different sprint and
depend on `escapeMarkdownLabel`, which does not exist at `59ff89a`.

⚠️ **The committed `c3b10-2-mutations.mjs` hardcodes `ROOT` to the primary worktree and
`writeFileSync`s into it.** Running it as-is mutates whatever that tree currently holds. The
2026-08-15 run used a copy with `ROOT` retargeted to the isolated worktree; both files were
hash-restored `IDENTICAL` afterwards.

---

As of 2026-08-11 (§1–§9): nothing committed, nothing pushed, `HEAD 59ff89a`, nothing staged.
C3-B.5 and C3-B.2 untouched. As of 2026-08-15: committed as `6c214c4` in the isolated verification
worktree only — **not pushed, not merged, not deployed**. C3-C not started.

**STOPPING FOR OWNER REVIEW.**
