# AUTORUN 2026-09-20 — nhật ký tiến độ (Owner đọc file này trước)

Nhánh `merge/main-into-v3`, code đầu job `61a0e9c`. Ngân sách 150 lượt: A 20 · B 12 · C 12 · D 6 · E 55 (RESERVED) · F 25 · G 0. Không push/deploy/xoá/đổi env prod/fetch HTML merchant/nới guard.

## CẦN OWNER (cập nhật liên tục)
- **Upstash KV**: A2 chạy trên `publicRateLimit`/`kvCounter` — cap toàn cục CHỈ khi `KV_REST_API_URL` + `KV_REST_API_TOKEN` (hoặc `UPSTASH_REDIS_REST_*`) có trên Vercel prod. Ghi chú P1-5 trong `publicRateLimit.ts` nói đã thấy 2 biến này trên Production (kiểm theo TÊN qua `vercel env ls`); hôm nay `vercel` bị policy chặn nên KHÔNG xác nhận lại được. Không có KV ⇒ mọi cap là per-instance (🚨 NOT PRODUCTION SAFE) — log `scope:"instance"`.
- **Migration `supabase/migrations/20260920100000_commerce_providers.sql` CHƯA apply** (không có kênh DDL từ session — owner dán vào SQL Editor). Chưa apply ⇒ app chạy theo code registry (Lazada/Vexere = pending ⇒ direct; log `tappyai_commerce_registry load_failed` 1 lần/60 s). Sau khi apply: **flip tier = `update public.commerce_providers set deeplink_enabled=…, tier=…, network=…, campaign_id=… where provider_id='…'`** — không deploy, không sửa code (cache 60 s).
- **Traveloka + Vietnam Airlines (Tier 1 theo owner)**: code KHÔNG có campaign id/wrapper (ghi chú registry: Traveloka "AT → Partnerize approved, wrapper chưa param-echo"). Hàng seed có `deeplink_enabled=true` nhưng `network/campaign_id/wrapper_template = null` ⇒ app log ERROR `deeplink_enabled_without_wrapper` và phát link DIRECT (không bịa tracked URL). Owner điền `network='accesstrade'+campaign_id` hoặc `network='template'+wrapper_template='https://…{url}…'` vào bảng.
- **`ACCESSTRADE_PUBLISHER_ID` trên Vercel prod**: không có ⇒ mọi wrapper Accesstrade = `unavailable` ⇒ direct (không lỗi, không kiếm). Không kiểm được hôm nay (`vercel` bị chặn).
- **DMX (Điện Máy Xanh)**: có trong code (pending), KHÔNG có trong danh sách owner ⇒ giữ active, Tier 2 direct — owner xác nhận giữ/bỏ.
- **Shopee/Lazada/DMX/Vexere approval**: Lazada + Vexere đã set Tier 1 theo owner (campaign id trong code); Shopee pending (Tier 2 direct).
- **Feed Accesstrade (B5)**: cron `/api/cron/feed-ingest` chạy hằng ngày nhưng BLOCKED tới khi có `ACCESSTRADE_API_KEY` + `ACCESSTRADE_FEED_ENDPOINT` (mẫu: `https://api.accesstrade.vn/v1/datafeeds?campaign={campaign}&format=csv`) trên Vercel — mỗi run ghi `blocked_no_credentials` vào `commerce_feed_runs`. Migration `20260920110000_commerce_feed_items.sql` cần apply. **D7 (hiển thị title/price/image từ feed)** vẫn TẮT — cần văn bản xác nhận quyền dữ liệu.
- **Env mới (tuỳ chọn, có default trong code)**: `CHAT_IP_BURST_PER_MINUTE=30`, `CHAT_USER_BURST_PER_MINUTE=20`, `PRO_DAILY_CHAT_CAP=300`, `SERPER_DAILY_CREDIT_CEILING=15000`. Không sửa được `.env.local.example` (deny rule của session) — owner thêm nếu muốn.

## BLOCKED (safe default đã áp dụng)
- **C2 `TRAVELPAYOUTS_TOKEN`**: không có trong `.env.local` audit (kiểm theo TÊN, không in giá trị) ⇒ `get_flight_prices` đi nhánh `notConfigured`: KHÔNG có giá vé, chỉ `booking_links` route có ngày (Trip.com + Traveloka). Safe default = câu trả lời trung thực "chưa có giá" + link route có ngày + hedge khi model tự nêu giá. Trip.com KHÔNG có feed giá vé máy bay trong code (chỉ deeplink) ⇒ không có nguồn giá thay thế; giá máy bay luôn hedge tới khi owner cấp token (Vercel prod cũng cần kiểm — `vercel` bị chặn).

## CORRECTION LOG
- **2026-09-20 09:40 (SEAST)** — Owner gửi "A3. COMMERCE PROVIDERS, LINK DESTINATIONS, AFFILIATE (replaces old A3)". Lúc nhận A3 CHƯA bắt đầu ⇒ áp dụng bản thay thế nguyên vẹn (A3.0–A3.7), không làm lại A1/A2, các phase khác giữ nguyên thứ tự. Danh sách provider của owner là nguồn có thẩm quyền (bảng đối chiếu ở cuối file).

## Lượt đã dùng: 29/150 (A: 9 · B: 6 · C: 6 · D: 8 = 6 ngân sách + 2 mượn từ E-reserve để xác nhận fix live — trong đó 1 lượt S7 bắn nhầm ở background, vẫn tính)

## Nhật ký theo item
| # | Item | Commit | Prose? | Verified by | Lượt | Ghi chú |
|---|---|---|---|---|---|---|
| A1(c) | **Pre-search**: route chạy `search_places` (cùng tool object đã wrap) TRƯỚC model khi có directive search-now; model nhận cặp tool-call/tool-result; stream client nhận `9:`/`a:` như cũ. Lock test đổi invariant: 1 `AI.stream` + ≤1 tool trước đó qua `tools.search_places.execute`, args từ code (presearch.ts) | `91a1916` | YES (place turn bỏ bước 1) | unit 37 (presearch + lock) · **live F7b**: `llmCalls 1`, presearch 1.8 s, tổng 13.9 s cold (cache write 26k) vs 16.3 s trước; prose đúng | 1 | Điểm buffer chính xác: `applyPlaceEnrichmentStreamFilter` transform — khi `bufferMode` (place/travel/V1/ticket), mọi `0:` sau tool bị giữ trong `mainText` tới `d:` → `emitReconstructed` (guard cả prose) rồi mới phát `8:` + prose. Phạm vi: place types (restaurant/cafe/spa/bar/attraction/cinema); **hotel + shopping vẫn 2 bước** (chưa làm) |
| A1(d)-ảnh | Ảnh chỉ cho 3 card trên fold (`PHOTO_ENRICHMENT_LIMIT` 8→3) | `f86d2fc` | NO | unit (photoDeferral) · reasoning: −5 credit/lượt hotel, −0.5–1 s | 0 | 5 card sau fold không ảnh tới lượt sau |
| A1(d)-gợi ý thêm | Lưu args search + tên đã hiện vào decision_evidence; "gợi ý thêm" replay cùng call qua pre-search (cache 30' ⇒ 0 credit warm) + directive loại tên đã hiện; block "evidence missing" chỉ cho shopping | `9e35c6c` | YES (follow-up "thêm") | unit 6 + wiring · **chưa live** (đo ở Phase F) | 0 | Android chưa echo `X-Decision-Evidence-Id` (web có) — item riêng |
| A1(a)+(b) | **Cards-first + progress thật**: ngay khi rows về, stream phát `8:` `tappy.progress.v1` (`found`, đếm rows) + frame place `preliminary` (bộ của engine, `ranked:false`, không pick/lý do/ảnh); tại `d:` phát `finishing` trước guard+ảnh; frame quyết định thay thế (reader 2 surface lấy frame CUỐI). Web: hint tiến độ thay dòng xoay chung; card hiện mid-stream chỉ từ frame server gửi (không từ recall cache). Android: `ChatStreamFrames` parse theo `kind`, `Progress` event, `streamingHint`/`streamingPlaces` dưới bubble đang stream | `b00a8cb` | NO | unit 9 (cardsFirstProgress) + wiring web + Android unit 4 (ChatStreamProgressFramesTest) · **live web run 6** (café, guest, cold): searching 1.4 s → found+cards 4.6 s → finishing 12.2 s → full 14.0 s · **live Android run 9** (food, guest): "Đang tìm chỗ quanh bạn…" ≤1.5 s, "Đã có 10 chỗ…" + card ≤5 s, full ~19 s (emulator) | 3 | Không stream câu đã validate: guard V1 (cap 6, 1 alternative, trailing question, grounding cắt theo tỉ lệ) KHÔNG prefix-stable ⇒ phát sớm = nới guard (cấm). Card #1 sơ bộ có thể khác card #1 cuối (run 3: SIZ SPA → Serene) — cờ `preliminary` + không nhấn mạnh; ratchet i18n +1 kế thừa từ `e7214ca` (`×` U+00D7) đã sửa |
| A1(b)-TTFB | **Byte đầu không chờ pre-search**: route trả response NGAY với frame `searching`; phần còn lại (search, 1 `AI.stream`, mọi filter, usage) chạy trong `finishTurn()` → `deferredBody`; lỗi bên trong thành `3:` + `d:` (không treo). Turn không pre-search gọi cùng closure | `43a32a2` | NO | unit 4 (deferred) + route tests 175 (helper `settled` tee 2 chunk, mock `AI.providerId`) · **live run 3** (spa, cold): TTFB 4,984 → **681 ms**; cards 4.3 s; full 13.7 s (không đổi) · run 8 (android headers, cache ấm): TTFB 146 ms, cards 147 ms, full 7.3 s | 2 | 🚨 Bẫy đo: request đầu tiên sau `git checkout` trên dev server = recompile ~7 s (Android run 7 thấy headers 7.6 s — KHÔNG phải lỗi client) |
| A1-directive | Café/bar gọi đúng loại (`quán cà phê`/cafe, `quán bar`/bar — trước đó pre-search café = restaurant); lượt "thêm/khác" KHÔNG có evidence row (guest: anon write bị từ chối) vẫn search (directive như first reply + tên đã nêu ở reply trước) — trước đó model trả lời từ trí nhớ, guard cắt 356/523 ký tự | `8259a54` | YES | unit searchNow 8 + consultative 65 file · live web run 5 (đo được lỗi) · fix chưa live (đo ở Phase F) | 1 | SavePlaceButton web gate khi streaming `1f0f0e3` (bubble giờ tồn tại từ frame đầu) |
| A2 | **Rate limit + spend caps**: per-IP 30/min qua `publicRateLimit` (KV khi cấu hình, fail-closed khi store sập; in-process khi không — ghi `scope`), per-account 20/min (mới), Pro 300 lượt model/ngày (mới; canned $0 miễn), MỘT cửa Serper `serperClient.ts` + counter ngày dùng chung `kvCounter.ts` (INCRBY+EXPIRE NX; fallback per-instance có cờ), trần 15.000 credit/ngày, đếm TRƯỚC khi gọi, WARN 80 %, ERROR `tappyai_alert serper_daily_ceiling` 1 lần/ngày, tool trả "không có rows" — turn vẫn trả lời; cron price-check cùng cửa | `f6a75a6`+`0910743` | NO | unit 7 (serperClient/kvCounter) + route behaviour 5 (chatCaps.route: 21st turn/account 429 dù đổi IP, 31st/IP 429 trước identity, Pro 301st 429 câu nhẹ nhàng, canned miễn, free không bị chạm) · **không live** (0 lượt) | 0 | **$ script lạm dụng có thể đốt**: TRƯỚC — Pro: 30/phút × 1440 = 43.200 lượt/ngày ≈ $1.500 model + ~170k credit Serper (~$170) *mỗi instance, mỗi account*; SAU — Pro ≤300/ngày ≈ $10,5 + Serper toàn hệ ≤15.000 credit/ngày (~$15); free 15/ngày; anon 5 trọn đời. Với KV vắng: cap per-instance (N lambda × cap) — nêu ở CẦN OWNER |
| A3.0 | **Đối chiếu danh sách owner vs registry code** (bảng dưới) — 3 sai lệch báo cáo GATE 0 được sửa: (1) Lazada/Vexere code `pending` → owner Tier 1 ⇒ seed `deeplink_enabled=true` với campaign id đã có trong code; (2) **Traveloka + Vietnam Airlines CÓ trong code** (adapter grammar + registry, GATE 0 nói "absent" là SAI — chỉ thiếu tracking config), Tier 1 theo owner nhưng không có campaign ⇒ direct + ERROR log tới khi owner điền; (3) DMX code có, owner không ⇒ Tier 2 direct, chờ owner | — | — | reasoning (đọc `providers.ts`) | 0 | Tổng 17 provider trong code = 16 của owner + DMX |
| A3.1 | **Registry = runtime config**: bảng `commerce_providers` (provider_id, active, deeplink_enabled, tier, network, campaign_id, wrapper_template) + `runtime.ts` cache 60 s (nguồn Supabase inject từ seam `attachCommerceLinks`, CCP không I/O); resolver hỏi `effectiveTracking(entry)` mỗi link, orchestrator hỏi `isProviderActive`; KHÔNG nhánh theo provider id (test kiến trúc grep); row bật deeplink mà không có campaign/template ⇒ DIRECT + ERROR (không bịa); network `template` generic (`{url}`) cùng param-echo | `ad59f2d` | NO | unit 9: flip Tier 2→1 đổi link direct→tracked SAU khi cache hết TTL, không restart; inactive ⇒ không adapter; source hỏng ⇒ giữ code defaults | 0 | Owner flip ở: SQL `update public.commerce_providers …` (xem CẦN OWNER) |
| A3.2 | `CCP_ENABLED = true`; Tier 1 wrap qua deeplink (Accesstrade `go.isclix.com/deep_link/<pub>/<campaign>?url=…`, param-echo bắt buộc), Tier 2 direct. Đọc trang merchant (event schedule Ticketbox) TẮT sau `CCP_MERCHANT_PAGE_READ_ENABLED=false` — luật owner "không fetch HTML merchant" | `902f383` | NO | unit (resolver/seam 67 + ccp 119) | 0 | Wrap thật cần `ACCESSTRADE_PUBLISHER_ID` (owner) |
| A3.3 | **Deepest link tuyệt đối**: bỏ Google Shopping `ibp=oshop` làm CTA mua; bỏ "search fallback" marketplace (row không có trang sản phẩm ⇒ KHÔNG CTA); hotel bỏ `booking_link` (searchresults) + `agoda_link` (homepage) + `search_url`, OTA listing không phải trang 1 khách sạn ⇒ DROP (trước: thay bằng search page); Vexere/dsvn homepage bỏ; máy bay chỉ giữ trang route CÓ NGÀY (Trip.com/Traveloka L2 = trang route), bỏ VNA/Vietjet L1 landing + Google Flights; food bỏ GrabFood search + BeFood homepage (CTA = trang riêng của quán qua evidence entity-scoped hoặc Commerce Link) | `902f383`+`9afae36` | NO | unit: 31 test cũ (quyết định 14/9) viết lại có ghi "superseded"; `outboundLink.ts` depth từ URL shape/Commerce Link depth | 0 | Hệ quả trung thực: shopping row không tìm được trang sản phẩm ⇒ card không có nút mua (trước: Google Shopping) |
| A3.4 | **Telemetry link outbound**: mỗi link commerce log `tappyai_commerce_link {provider,tier,tracked,depth,kind,domain,host}`; homepage/search/intermediary ⇒ `console.error shallow_commerce_link`; counter UNTRACKED theo provider/ngày `commerce:untracked:<provider>` (kvCounter) ⇒ dòng `tappyai_commerce_untracked {provider,today}` mỗi lần phát — query log xếp hạng provider đáng chase | `902f383` | NO | unit (commerceActions 3 file) | 0 | Rank thật cần traffic prod |
| A3.5 | **Thiết bị**: emulator không cài app merchant ⇒ "mở app" KHÔNG đo được (web landing thôi); 3 link mở trên Chrome emulator: **Trip.com** hotel detail → đúng khách sạn (Nordic Resort), popup app promo, không login wall, **ngày ở KHÔNG áp dụng trên mobile web** (hiện hôm nay/1 đêm); **Traveloka** fullsearch SGN→HAN → đúng route + bảng giá, **ngày lệch** (10-10-2026 → hiện 22/9); **Vexere** trang route Hà Nội→Sa Pa → đúng route, 23 nhà xe, cookie + notification prompt, không login. Tracked vs direct: không so được (không publisher id) | — | — | live device 3/16 (0 lượt model) | 0 | Mất tracking do link quality: CHƯA có ca nào (không có wrapper hoạt động để so) |
| A3.6 | GrabFood/ShopeeFood chỉ là CTA trên card (trang riêng của quán); prose không có dòng "ShopeeFood · GrabFood · BeFood" trên surface có card — `decisionSurface.test` (android+web) đã khoá `shopeefood.vn`/`food.grab.com` không xuất hiện trong prose | có sẵn | NO | unit | 0 | |
| A4 | **Fence tool result**: mọi tool result (và pre-search) qua `gateTools` → `wrapToolResultAsData`: neutralise marker trong MỌI string, header DATA là key đầu, closer key cuối; test cấy lệnh vào title sản phẩm + review snippet ⇒ không đóng/mở/relabel span | `26294b8` | NO | unit 6 + fenceBoundary | 0 | Không claim model tuân thủ — tính chất cấu trúc |
| A5 | `fn_entry` log `gps {lat,lng}` 2 chữ số thập phân + `centeredOnUser` | `b0b7bdc` | NO | unit 2 | 0 | |
| B1 | **Hard-constraint gate áp cho shopping**: `search_products` được chấm theo ShoppingConstraints (`classifyShoppingGaps`): brand/size/variant có listing vouch hay là gap; `in_stock` + `recipient` LUÔN là gap khi user hỏi (feed không có stock/không xác minh được người nhận); annotate `_tappy_hard_gaps`/`_tappy_evidence_note` (từ vựng sản phẩm); heads-up "kiểm tra trên trang bán trước khi đặt". `hard_not_applicable` không còn log cho search_products | `9c1f07a` | YES | unit 17 (gate) + 41 (constraints) · **live run 15**: model tự nêu "chưa xác nhận được màu sắc/phiên bản… còn hàng" ✓ | (chung B) | B1+B2 chung 1 commit (ràng buộc chặt nhau) |
| B2 | **Validator mở rộng**: 7 → 27 họ sản phẩm (nước hoa, robot hút bụi, máy lọc không khí, máy lạnh, tủ lạnh, loa, mỹ phẩm, giày, quần áo, túi, console, xe đạp, đồ bé, sách, nội thất…); danh từ không biết sau động từ mua ⇒ `unknownType` = gap + WARN log; ràng buộc mới size / màu-dung tích / còn hàng / người nhận; listing NÊU size/màu khác ⇒ loại, im lặng ≠ loại. **Phát hiện live (run 10)**: need profile có lexicon riêng chỉ 5 họ điện tử ⇒ "Máy lọc không khí" = domain null ⇒ `v1Active:false`, model fast, không gate, không backstop — đây là nguyên nhân gốc S7. Hai lexicon nay là một (`PRODUCT_TYPES` dùng chung, subject tiếng Việt) | `9c1f07a`+`15f32a7` | YES | unit; **live run 13**: `v1Active:true`, frame shopping, gate `attributes` chạy | 2 | "sách" trần trụi bị bỏ (đụng "ngân sách"); "mua" trong "nhảy múa" bị loại |
| B3 | **Grounding PRODUCT unit**: tên bold không có trong rows đã bị grounding gate cắt (chứng minh trên S7 shape); giá không có row structured ⇒ HEDGE 1 dòng (không cắt) `money hedged_unverified`; bỏ qua khi budget line đã nói; **phát hiện**: turn shopping có chữ "phòng" bị snippet-price guard (của PLACE) cắt giá sản phẩm ⇒ tắt guard đó trên turn shopping | `de2458a`+`4b922bf` | YES | unit 3 (shoppingListGuard) · live run 15 hedge hiện | 0 | |
| B4 | **S7**: money guard R3 → TỈ LỆ (cắt clause chứa số tiền, giữ dòng sản phẩm; cắt cả câu chỉ khi clause = câu; stub "bạn nên." ⇒ cắt câu); grounding gate nhận `cardRenders` ⇒ không nói "xem thẻ bên dưới" khi không có card; backstop `shopping_none` khi body rỗng + không marker | `02e04e3`+`4b922bf` | YES | unit 4+3 · **live run 13** (S7 query): raw có "Mình chọn **Máy lọc không khí Daikin**…", gate chỉ cắt 1 tên không khớp (Kärcher AF 20) ⇒ pick còn — S7 shape PASS 1/1 (gate E chấm lại 2×) | 3 | run 11/12 = trước fix need-profile (v1 off) |
| B5 | **Feed Accesstrade**: ingester `feedIngest.ts` (ngoài CCP; key đọc trong `ccp/tracking/accesstradeFeedAuth.ts` theo rule kiến trúc), D6 giữ nguyên (`decideFeedSource`), cron + bảng `commerce_feed_items`/`commerce_feed_runs`, merchant = Tier 1 có campaign theo runtime registry (đổi bảng = bật ingest, không sửa code); `feedHints.ts` = nguồn discovery ĐẦU TIÊN cho shopping (trang sản phẩm merchant, 0 credit Serper, cùng identity bar §8). D7 OFF | `075c823` | NO | unit 7 · live: BLOCKED (không credential) | 0 | Feed không có stock/brand ⇒ không bao giờ claim còn hàng |
| B-live | Run 14 (nước hoa, có ràng buộc): pick bị cắt (guard nào chưa rõ — sink audit lúc đó chỉ ghi khi grounding cắt) ⇒ thêm sink ghi text sau TỪNG guard (`196dea9`); run 15 cùng câu: pick còn, alternative, hedge màu/phiên bản/còn hàng, budget line — đúng hình dạng | — | — | live 2 | (đã đếm) | Biến thiên model giữa 2 run; E-gate chấm 2× |
| A-gate | Full web suite `npm test`: **13.732 passed / 69 skipped, required suites OK**; `tsc -p` sạch; Android `testDebugUnitTest` exit 0; architecture guard ĐÃ có trong default suite (`scripts/architecture/controllerRules.test.ts` baseline) | — | — | run log `scratchpad/npmtest-A2.log` | 0 | |
| C1 | **Mọi GIỜ và GIÁ trong prose của unit SCHEDULE+TICKET phải trace tới row đã fetch**: travelGuard nhận `evidenceTimes` (giờ khởi hành đọc từ snippet xe khách/tàu trong tool result) — câu nêu giờ có trong evidence GIỮ, giờ không có ⇒ cắt câu + 1 dòng hedge "Giờ chạy và giá vé cụ thể mình chưa xác nhận được… bạn xem trên trang đặt vé"; `TIME_RE` nhận cả dạng `22:00`; giá vé xe/tàu từ snippet → `travelFares`. Xe khách/tàu KHÔNG bị pre-search nhầm thành nhà hàng (`TRANSPORT_REQUEST` ⇒ directive null) | `e7c66ff`+`47bc996` | YES | unit 33 (travelGuard) + searchNow · **live run 16** (xe khách SG→Đà Lạt): Vexere route có ngày, giờ/giá nêu = từ snippet; **run 17** (máy bay SGN→HAN 10/10 ×2): tool `notConfigured` ⇒ prose trung thực "chưa có kết quả giá vé", Trip.com + Traveloka route có ngày, `quantity=2` | 2 | Run 17 phát hiện lỗi cắt dòng link Trip.com → C1-fix |
| C1-fix | **Bug guard**: `TIME_RE` kết thúc bằng `\b` sau "h" — JS coi "à" là non-word ⇒ "nhập thông tin **2 hành khách**" đọc thành giờ "2h" trong câu có "chuyến bay" ⇒ cắt cả dòng link Trip.com (64 ký tự; sink ghi nhầm cho `place_claim` vì travel guard chạy trong bucket afterSpec→afterPlaceGuard). Sửa: đơn vị giờ phải kết thúc bằng non-letter/non-digit | `cb5b0d8` | YES | unit (đỏ trên HEAD~, xanh sau) · **live run 18**: 0 ký tự bị cắt ở MỌI stage; 2 link route có ngày + "nhập thông tin 2 hành khách" còn nguyên | 1 | Cùng bẫy Unicode `\b` mà AVAIL_RE đã ghi chú |
| C2 | `get_transport_options` **đã chạy live** (run 16: rows Vexere, giờ+giá từ snippet); `get_flight_prices` **0 rows là do thiếu `TRAVELPAYOUTS_TOKEN`** (nhánh `notConfigured`, không phải lỗi parse) ⇒ BLOCKED (xem mục BLOCKED); Trip.com feed giá bay KHÔNG tồn tại trong code ⇒ hedge | — | — | live 2 (đã đếm ở C1) + reasoning (`travel.ts:110`) | 0 | Owner: `TRAVELPAYOUTS_TOKEN` trên Vercel prod + audit |
| C3 | **Lịch chiếu rạp = trả lời trung thực, không crawl**: (1) trigger turn `mentionsTickets` thêm từ vựng rạp/giá vé ("vé bao nhiêu", "chiếu phim", "rạp phim", CGV/Lotte/Galaxy/BHD) — TRƯỚC: câu "tối nay rạp CGV… chiếu phim gì, mấy giờ, vé bao nhiêu?" không khớp rule nào ⇒ turn chạy KHÔNG guard giá ⇒ "thường 80k-150k" bịa lọt (run 19); (2) travelGuard `unit:'ticket'`: giá vé/suất chiếu không có nguồn ⇒ cắt + 1 hedge "Suất chiếu và giá vé cụ thể mình chưa xác nhận được… bạn xem trên trang rạp hoặc trang bán vé"; (3) placeClaimGuard: câu khai "Mình chưa có lịch chiếu" và câu chỉ NÊU lịch ("Lịch chiếu tối nay tại CGV…") không còn bị coi là claim BÁN VÉ (trước: cắt sạch, còn 1 dòng); câu lịch có GIỜ ("Avatar lúc 19h30") = availability claim ở mọi scope | `d8fd1f6` | YES | unit 139 (ticket suites) · **live run 21**: prose trung thực + link rạp CGV Vincom Đồng Khởi (deep, entity-level) + CTA "Xem lịch chiếu CGV"; câu "8h–22h, giá 80k-150k" bị cắt; hedge 1 lần; TTFB 3.6 s / full 10.8 s (web_search, không pre-search) | 3 | Run 20 lộ 2 bug stream → C3-fix. Wording model "Mình cần vào trang CGV chính thức để xem" hơi vụng nhưng trung thực; KHÔNG có card rạp (turn đi `web_search`) ⇒ Phase D |
| C3-fix | **2 bug stream**: (a) `safeFlushPoint` coi ĐẦU/CUỐI link markdown là ranh giới câu (`sentenceSpans` tách link thành span riêng) ⇒ phát sớm nửa câu "Bạn có thể vào **[trang CGV](…)" rồi guard cắt clause giá phía sau ⇒ reply lặp 2 câu mở đầu + `**` mồ côi; sửa: chỉ phát tại dấu kết câu/xuống dòng/hết segment; (b) phần phát sớm gửi `unemphasizeLinks(slice)` + theo dõi `flushedSent` (byte đã gửi) để phép trừ cuối khớp text đã settle (trước: `**[link]**` phát sớm ≠ `[link]` sau strip ⇒ belt `startsWith` hỏng ⇒ gửi lại toàn bộ); (c) fragment "đặt vé trực tiếp trên **" (bị link cắt sau giới từ) = framing "xem ở đâu", không phải claim bán vé (trước: cắt fragment, để 2 link mồ côi) | `5b72c54` | YES | unit 3 mới (đỏ trên HEAD~) + ai/route 3.946 xanh + tsc · live run 21: không lặp, không `**` mồ côi | (run 20+21 đã đếm ở C3) | Bug (a)/(b) là của mọi turn progressive có link inline trong câu mở đầu — không riêng rạp |
| C4 | Card giữ nguyên là card địa điểm (ticket unit không đụng frame `tappy.places.v1`/`[TAPPY_PLACES]`: guard chỉ chạy trên prose, marker được `earliestMarker` bảo vệ); chi tiết vé/suất = prose (hedge + link) | có sẵn + `d8fd1f6` | NO | unit (entertainmentNonPlaceTurn: CTA_BUTTONS còn nguyên sau cắt) · reasoning | 0 | Card rạp thật (search_places cho cinema) = Phase D |
| C-gate | Full web suite sau Phase C: **13.780 passed / 69 skipped, 1 failed** = pin số cron Vercel (9) mà B5 đã nâng lên 10 (`/api/cron/feed-ingest`) — quyết định có chủ đích, ghi vào test; `tsc -p` sạch. Android unit: KHÔNG chạy lại (code Android không đổi từ A-gate; chạy lại ở E-gate) | `d003d02` | NO | run log `scratchpad/npmtest-C.log` + outbox 44 xanh | 0 | |
| D | **Karaoke / rạp / công viên nước / thủy cung = card địa điểm**: (1) need profile biết chuỗi rạp (rạp CGV/Lotte/Galaxy/BHD/Cinestar), công viên nước, thủy cung, karaoke, bowling/bida/escape room/trượt băng ⇒ domain entertainment/places (trước: "rạp CGV" = domain null ⇒ `v1Active:false`); "Galaxy" là điện thoại trừ khi sau "rạp"/trước "cine"; (2) `detectForcedTool` các loại này ⇒ `search_places` (trước: rơi xuống rule "?" ⇒ web_search); (3) directive pre-search: rạp ĐƯỢC NÊU TÊN ⇒ search chính xác `cgv vincom dong khoi`/cinema (không cần quận/GPS — decision frame coi tên venue là location; `namedCinemaQuery` trong intent.ts); loại ⇒ query đúng loại (`rạp chiếu phim`/cinema, `quán karaoke`, `công viên nước`, `thủy cung`, `khu vui chơi giải trí`/attraction) thay vì "địa điểm vui chơi giải trí" chung; (4) ticket-unit guard chạy trên MỌI turn vé (trước snippet-price guard) với giá/giờ trong snippet của chính venue làm evidence | `e637b23` | YES | unit (searchNow/needProfile/intent/actionability mới; ai+route 3.963 xanh; tsc) · **live run 23** (rạp CGV Vincom Đồng Khởi): card 1 = pick "CGV Vincom Center Đồng Khởi" 4.3⭐ (4.500 đánh giá) từ row, prose trung thực "không thể cập nhật realtime", CTA trang rạp (deep), 0 giá/giờ bịa; cards sơ bộ 5.2 s / full 11.9 s · **run 24** (karaoke nhóm 8, 150k/người): 8 card, pick Karaoke MEI = card 1, giờ/giá band từ row, alternative Avatar · **run 28** (công viên nước gia đình): card 1 = pick **Công Viên Nước Đầm Sen** (7 km, 4.3⭐/36.007), giá vé "chưa tìm thấy" + SĐT từ row + website riêng · **run 29** (thủy cung): 5 row, pick Đầm Sen (Sài Gòn không có thủy cung công cộng thật; "Thủy Cung" 4.2⭐ là alternative) — câu "vừa có khu thủy cung" của model là claim chưa có bằng chứng ⇒ đưa vào E1/E3 | 8 (22–29) | Run 22: canned clarify "Tầm giá? Mấy người?" cho câu hỏi về rạp ĐÃ NÊU TÊN ⇒ `7d143ab` (named venue = actionable); run 25/26: "công viên nước có khu trẻ em" @14z quanh Q1 ⇒ "Khu Vui Chơi Nước Nóng Nora"/"Kho Nước Suối" (tiệm giao nước!) ⇒ `1d0de33` bỏ modifier kids cho loại gia đình + `6525337` attraction/cinema tìm ở 12z (thành phố) + thủy cung/công viên nước không bị canned clarify. Phase D vượt 2 lượt (mượn E-reserve) |
| A1-tổng | **Trước/sau (cold, place turn)**: màn trắng 13–17 s → tiến độ thật 0.7–1.4 s, card sơ bộ 4.3–5.0 s, full 13.7–14.0 s (web); Android ≤1.5 s / ≤5 s / ~19 s. Hotel: progress `found` + không card sơ bộ (hotel không có card); shopping/hotel vẫn 2 bước (ngoài scope pre-search) | — | — | — | — | Entertainment/cinema đi cùng đường pre-search (type attraction/cinema) — chưa đo live riêng (ngân sách), đo ở Phase D/F |


## A3.0 — Bảng đối chiếu provider (owner 2026-09-20 vs code registry `providers.ts`)
| Provider | Owner tier | Code trước A3 | Seed `commerce_providers` | Link sau A3 | Tracked? | Ghi chú |
|---|---|---|---|---|---|---|
| Lazada | 1 | mvp, tracking `pending` | deeplink=true, accesstrade 5087153089503673507 | trang sản phẩm (discovery `site:lazada.vn`) | có khi publisher id + row | GATE 0 nói pending — owner thắng |
| CellphoneS | 1 | handoff_only, `approved` | deeplink=true, 6259155740535091857 | trang sản phẩm (passthrough L4) | có | |
| Trip.com | 1 | mvp, `approved` | deeplink=true, 6455552313033835511 | hotel detail có ngày / flight route có ngày | có | device: ngày không áp trên mobile web |
| Traveloka | 1 | mvp, KHÔNG tracking config | deeplink=true, network=null | flight fullsearch có ngày / hotel page | **KHÔNG** (thiếu campaign/template) → ERROR log | GATE 0 "absent from code" SAI |
| Vexere | 1 | mvp, `pending` | deeplink=true, 5222734619328835827 | trang route có ngày (CCP) | có khi publisher id | homepage đã bỏ |
| Vietnam Airlines | 1 | mvp, KHÔNG tracking | deeplink=true, network=null | (L1 landing bị bỏ) ⇒ không link | **KHÔNG** | GATE 0 "absent" SAI; cần grammar sâu hơn + campaign |
| Klook | 1 | mvp, `approved` | deeplink=true, 4704521809526929067 | activity page (discovery) | có | |
| TikTok Shop | 1 | mvp, `approved` | deeplink=true, 6648523843406889655 | product page | có | |
| Shopee | 2 | mvp, `pending` | deeplink=false | product page (discovery) hoặc KHÔNG | không | search fallback đã bỏ |
| Vietjet | 2 | mvp, không tracking | deeplink=false | (L1 landing bị bỏ) ⇒ không link | không | |
| Booking.com | 2 | mvp, không tracking | deeplink=false | trang khách sạn riêng (row) | không | searchresults đã bỏ |
| Agoda | 2 | mvp, không tracking | deeplink=false | trang khách sạn riêng (row) | không | homepage đã bỏ |
| GrabFood | 2 | handoff_only | deeplink=false | trang quán (evidence/CCP) hoặc KHÔNG | không | search đã bỏ |
| ShopeeFood | 2 | handoff_only | deeplink=false | trang quán (evidence/CCP) hoặc KHÔNG | không | |
| Ticketbox | 2 | mvp, không tracking | deeplink=false | event page (discovery); schedule read TẮT | không | |
| CGV | 2 | mvp, không tracking | deeplink=false | film page (grammar) | không | showtimes: "không có" + link rạp |
| DMX | (không có) | mvp, `pending` | deeplink=false (chờ owner) | product page | không | code có, owner không |

**Per vertical (đích hôm nay → sau A3):** Ăn uống: GrabFood/BeFood search+homepage → trang quán riêng hoặc không CTA · Mua sắm: Google Shopping `ibp=oshop` + search marketplace → trang sản phẩm merchant (discovery Serper `site:`) hoặc không CTA · Du lịch: Booking searchresults / Agoda homepage / Vexere+dsvn homepage / Google Flights → trang khách sạn riêng, route có ngày (Trip.com/Traveloka/Vexere), không có = không link · Spa: không đổi (website/maps/call) · Giải trí: CGV film page, Klook activity, Ticketbox event (không đọc trang) — CCP nay BẬT nên các link này thực sự phát.
