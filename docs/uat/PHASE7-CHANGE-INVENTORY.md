# Phase 7 — change inventory for review (worktree frozen 2026-09-22)

Worktree `D:/Claude/Projects/TappyAI/tappyai-mvp/.claude/worktrees/g1-place-guard`, branch `uat/release-audit-2026-09`, HEAD `7487b4d`.
Nothing staged, nothing committed, nothing pushed. `git diff --numstat`: 53 modified files (+1 701 / −328); 71 untracked files.
Companion report with the verification evidence: `docs/uat/PHASE7-AUDIT.md`.

## 0. Noise to exclude from the commit

| Path | Why |
|---|---|
| `docs/audit/lang-detect-results.json`, `p15-truncation-repro.json`, `prompt-shape.json`, `route-signals.json` | Regenerated by the test run (timestamps, ±1 token counts, the "current time" line). No Phase 7 content. `git checkout -- docs/audit` before committing. |
| `docs/uat/evidence/fix-dev.log` | Dev-server log from the previous session (already untracked before Phase 7). |

## 1. Modified files (49 after excluding §0)

### Issue 1 — composer CSS
| File | Change |
|---|---|
| `src/app/globals.css` | +388 lines, two blocks appended byte-exact from `3fe8c12`: `.v3-post-*` (composer, 202 lines, from L4261) and `.v3-pp-*` (Explore profile, 186 lines, from L4463). No existing rule touched. |

### Issue 2/4 — recommendations naming
| File | Change |
|---|---|
| `src/lib/i18n/v3/web.ts` | `v3.tool.suggest` → "Gợi ý cho bạn"/"Suggested for you"; `v3.tool.safety` → "Cảnh báo lừa đảo"/"Scam Alerts"; Home strip `v3.home.forYouTitle/Sub` → "Hỏi Tappy thử"/"Try asking Tappy" + subtitles; new `v3.home.forYouAction` ("Mở trò chuyện"/"Open chat"). |
| `src/components/v3/V3Shell.tsx` | Sidebar capability row: label key `v3.nav.search` → `v3.tool.suggest`, icon `Search` → `Sparkles` (href unchanged `/recommendations`). |
| `src/app/HomeV3.tsx` | Prompt strip action: `{seeAll → /recommendations}` → `{forYouAction → /chat}`; comment. |
| `src/app/recommendations/page.tsx` | Back: `router.push('/')` → `goBack(router, '/tools')`, label `recommendations.home` → `common.back`. |

### Issue 3 — Scam Shield naming + nav
| File | Change |
|---|---|
| `src/lib/scam-shield/knowledge/bocongan2026.ts` | 3 VI `whatToDo` strings: "Scam Shield" → "Cảnh báo lừa đảo". |
| `src/components/Header.tsx` | `goBack` uses `lib/nav/inAppBack.goBack` when `backFallbackHref` is set (was `history.length <= 1`). |
| `src/app/currency/page.tsx`, `src/app/split-bill/page.tsx` | Back glyph `<Link href="/">` → `<InAppBackButton fallbackHref={SMART_TOOLS_HREF}>`; `Link` import dropped. |
| `src/app/boi/BoiLandingView.tsx`, `src/app/viet-content/VietContentView.tsx` | `<Header backHref="/">` → `backFallbackHref={SMART_TOOLS_HREF}`. |
| `src/app/layout.tsx` | Mounts `<NavHistoryTracker />`. |

### Issue 4 — group
| File | Change |
|---|---|
| `src/app/group/new/GroupNewForm.tsx` | `backHref="/"` → `backFallbackHref={SMART_TOOLS_HREF}`. |
| `src/app/group/[id]/page.tsx` | Same Back change; group avatar (image or placeholder), creator-only camera control + hidden file input, `handleAvatarSelect` → `POST /api/group/[id]/avatar`, error line; `Group.avatar_url`. |
| `src/app/api/group/route.ts` | GET selects `avatar_url`, nulls it unless `isServableMediaUrl`. |

### Issue 5 — music library
| File | Change |
|---|---|
| `src/app/api/music/tracks/route.ts` | GET restored (browse, `force-dynamic`); POST stays 410. |
| `src/app/api/music/tracks/[id]/route.ts`, `search/route.ts`, `categories/route.ts`, `providers/route.ts` | GET restored from `919736a~1`; `[id]` and `providers` gained `force-dynamic` (param-only GET was framework-cached and served stale titles). |
| `src/app/api/reviews/route.ts` | `music` body accepted only when `getTrack()` (library rows only) resolves it, stored as `{version:1, trackId, startSec, volume, origin:'attached'}`; any other id → 410 (unchanged answer for a borrowed sound). `ReviewMusic` type + import of `@/modules/music/server`. |
| `src/app/api/reviews/feed/route.ts`, `mine/route.ts`, `[id]/route.ts` | `music` column back in the SELECT lists. |
| `src/app/reviews/feedShared.tsx` | `Review.music` type; attached-track resolution via `useMusicTrack` → `VideoPlayer hasSound/soundUrl/soundVolume`; `<ReviewMusicCredit>` under the caption. No sound disc, no SoundSheet. |
| `src/app/reviews/[id]/ReviewDetailView.tsx` | Same attached-track playback on the hero player; `<ReviewMusicCard>` (attribution) under the body; `music` in the local type. |
| `src/app/reviews/new/page.tsx` | "Thêm nhạc nền" chip, `MusicPickerSheet`, `SelectedMusicCard` (with credit / "no longer in the library" state), `payload.music`. No `?sound=` deep link. |
| `src/app/music/page.tsx` | Approved V3 library page restored from `21cc9cf~1` minus the `/music/upload` action and the "upload your Original Sound" pill; Back → in-app (fallback `/tools`). |
| `src/lib/i18n/w2/reviewNew.ts` | +`reviewNew.musicUnavailable` (VI/EN). |

### Issue 7/8/9 — profile / QR / clip Back
| File | Change |
|---|---|
| `src/app/profile/qr/QRProfileView.tsx` | Back `<Link href="/profile">` → `goBack(router,'/profile')`; share → `<ShareMenu url={absoluteUrl('/users/<id>')}>`; download → `renderBrandedQrCard` (branded PNG). |
| `src/components/QRProfileButton.tsx` | Modal share → `<ShareMenu>` with the canonical URL (was `navigator.share`/clipboard). |
| `src/app/reviews/[id]/ReviewClipView.tsx` | close → `goBack(router,'/reviews')` (was `document.referrer`). |
| `src/app/reviews/[id]/ReviewBackButton.tsx` | → `goBack(router,'/reviews')` (was `history.length > 1`). |

### Closure item 3 — V3 profile skin (restored from `3fe8c12`, reconciled)
| File | Change |
|---|---|
| `src/app/users/[id]/UserProfileView.tsx` | Renders `PublicProfileView`; keeps HEAD's `/users/<me>` → `/profile` redirect; anonymous session = visitor; Back via in-app stack (fallback `/reviews`). |
| `src/app/api/users/[id]/route.ts` | `readPublicProfile` with the 42703 schema bridge; serves `bio`, `cover_url`. |
| `src/app/api/profile/route.ts` | Merged: `readOwnProfile` (bio/cover with schema bridge) in parallel with the existing age/demographics reads; PATCH writes `bio` to `profiles` (+metadata as before) and can clear `cover_url`; new `uploadCover` (multipart `cover`, anon refused, sniffed, `MAX_PHOTO_SIZE_MB`, `covers/<uid>-<suffix>`), POST dispatches avatar vs cover by field. |
| `src/app/profile/edit/page.tsx` | Cover control (upload/remove) when `cover_url` is present in the GET. |
| `src/app/profile/ProfileView.tsx`, `src/app/profile/page.tsx` | `coverUrl` prop → hero background when set. |
| `src/lib/media/key.ts` | `covers` added to `ALLOWED_MEDIA_PREFIXES`. |
| `src/lib/i18n/w4/misc.ts` | +16 profile/cover strings (VI/EN). |

### Tests modified (9)
`src/app/api/profile/patchMetadataFallback.test.ts` (bio is a column again), `src/app/boi/boiLandingV3.test.tsx`, `src/app/group/new/groupNewV3.test.tsx` (Back = fallback `/tools`, not `backHref="/"`), `src/app/profile/profileHub.test.tsx` (cover), `src/app/profile/qr/qrProfile.test.tsx` (ShareMenu + branded download pins), `src/app/reviews/[id]/reviewClipViewClose.test.tsx` (stack signal, referrer no longer decides), `src/components/legalBackNavigation.test.tsx` (in-app depth helper), `src/lib/auth/socialWriteAccess.test.ts` (+group avatar route in the boundary).

## 2. Untracked files (69 after excluding §0)

| Group | Files |
|---|---|
| Nav | `src/lib/nav/inAppBack.ts` (+ `inAppBack.test.ts`), `src/components/NavHistoryTracker.tsx`, `src/components/InAppBackButton.tsx` |
| Group avatar | `src/app/api/group/[id]/avatar/route.ts` |
| QR | `src/lib/qr/brandedCard.ts` |
| Music module (restored from `c584367~1`, then edited) | `src/modules/music/**` — 46 files. Edited vs the snapshot: `repository/musicRepository.ts` (admin client, `LIBRARY_TYPES` filter, `license`/`source_url`, UGC writers removed), `services/musicService.ts` (server reads only), new `services/musicClient.ts` (fetch) and `services/musicHelpers.ts` (pure, +`attributionLine`), `hooks/*` → client, `components/{MusicCategoryTabs,MusicPickerSheet,MusicSelectionPanel}` → helpers import, `index.ts`/`server.ts` barrels, `types/track.ts` (+license, sourceUrl), tests `categoryLabel.test.ts`, `musicTrackCard.test.tsx` fixture. |
| Music UI | `src/app/reviews/ReviewMusicCard.tsx` (attribution card + `ReviewMusicCredit`; no `/sound` link) |
| Profile skin | `src/app/users/[id]/PublicProfileView.tsx`, `ownerCollections.ts`, `publicProfile.test.tsx`, `src/lib/profile/cover.ts`, tests `src/app/api/profile/coverAndBio.test.ts`, `src/app/api/reviews/mine/selfScope.test.ts`, `src/app/api/users/[id]/publicFields.test.ts`, `src/app/profile/edit/editProfileCover.test.tsx` |
| Docs / evidence | `docs/uat/PHASE7-AUDIT.md`, `docs/uat/PHASE7-CHANGE-INVENTORY.md` (this file), `docs/uat/evidence/soundhelix-audio-examples-2026-09-22.html` |
| Migrations | see §3 |

## 3. Migrations

| File | Effect | Applied to audit DB `zdaprdfgpbpnxyofagmc` | Rollback |
|---|---|---|---|
| `20260922_groups_avatar_url.sql` | `groups.avatar_url text` (additive, idempotent) | yes | `rollback/20260922_groups_avatar_url_rollback.sql` |
| `20260915_profile_public_presentation.sql` (from the 3fe8c12 snapshot) | `profiles.bio`, `profiles.cover_url` (additive, idempotent; code has a 42703 bridge either way) | yes | none in file; `ALTER TABLE public.profiles DROP COLUMN IF EXISTS bio, DROP COLUMN IF EXISTS cover_url;` |
| `20260922_music_soundhelix_attribution.sql` | UPDATE of the 14 rows whose `audio_url ~ '^/music/soundhelix-song-\d+\.mp3$'`: title `SoundHelix Song N`, artist `T. Schürger · SoundHelix`, `license`, per-song `source_url`. Touches nothing else (Jamendo rows untouched). | yes | none needed for prod (rows exist only where the seed was run); to undo on the audit DB re-run `20260705_seed_music_demo_catalog.sql` + `20260706c_repoint_music_audio_local.sql` |
| Existing, applied to the audit DB during this work (not new files) | `add_music_attribution.sql`, `20260705_seed_music_demo_catalog.sql`, `20260706c_repoint_music_audio_local.sql`; `INSERT music_providers (internal, pixabay)` (the statement from `20260704_add_music_module.sql`) | yes | `DELETE FROM music_tracks WHERE artist LIKE '%SoundHelix%'` |
| Audit-DB seed rows (not migrations) | 4 `reviews` rows `[UAT clip 1–4]` (real prod clips) | yes | `DELETE FROM reviews WHERE body LIKE '[UAT clip %'` |

Not touched: `20260921_music_tracks_lockdown.sql` stays in force (no anon/authenticated grant on `music_tracks`); `20260915b_review_likes_private.sql` from the snapshot was deliberately NOT brought in.

## 4. API changes

| Route | Change |
|---|---|
| `POST /api/group/[id]/avatar` | **New.** Auth + non-anonymous; 404 unknown group; **403 non-creator before any byte is stored**; multipart `avatar`; magic-byte sniff; 3 MB; `putMedia('avatars/group-<id>-<suffix>.<ext>')`; UPDATE `groups` scoped `id AND creator_id` with `.select()` — zero rows = 500, never a fake success. Returns `{avatar_url}`. |
| `GET /api/group?id=` | + `avatar_url` (servable URLs only). |
| `GET /api/music/tracks`, `/tracks/[id]`, `/tracks/search`, `/categories`, `/providers` | **Restored** (were 410). Server-only repository over the service-role client; tracks filtered `is_active AND music_type IN ('royalty_free','licensed')`; `+license`, `+sourceUrl` in the track shape. All `force-dynamic` (search is dynamic by `req`). |
| `POST /api/music/tracks`, `/api/upload/audio`, `/api/sound/*`, `/api/music/tracks/[id]/report` | Unchanged — still 410. |
| `POST /api/reviews` | `music` accepted iff the id is a library track (server re-check); else 410. `origin:'attached'` stored. Own-audio auto-registration stays removed. |
| `GET /api/reviews/feed`, `/mine`, `/[id]` | `music` column returned again. |
| `GET /api/users/[id]` | + `bio`, `cover_url` (schema-bridged). |
| `GET/PATCH/POST /api/profile` | GET + `bio` from the row, `cover_url` key when the column exists; PATCH writes `bio` to `profiles` and accepts `cover_url: null` to clear; POST accepts multipart `cover` (new) alongside `avatar`. |

## 5. UI changes

- `/reviews/new`: approved V3 composer design renders again (CSS); "Thêm nhạc nền" chip → library picker → selected card with credit.
- `/tools`, sidebar: "An toàn" → "Cảnh báo lừa đảo"; "Gợi ý"/"Search" → "Gợi ý cho bạn" (one name for `/recommendations`); sidebar row icon.
- Home: prompt strip retitled "Hỏi Tappy thử", action "Mở trò chuyện" → `/chat` (was "Xem tất cả" → `/recommendations`).
- `/scam-shield`: knowledge-base copy no longer says "Scam Shield" in VI.
- Back on `/currency`, `/split-bill`, `/boi`, `/viet-content`, `/recommendations`, `/music`, `/group/new`, `/group/[id]`, `/profile/qr`, clip route and clip viewer: pops in-app history, falls back to the declared parent.
- `/group/[id]`: group picture + creator-only change control.
- `/music`: approved library page (no UGC upload action/pill).
- Feed / clip detail: attached library track plays over the clip; credit line "♪ title · artist · licence"; detail card links the source page. No "use this sound".
- `/profile/qr` and the profile QR modal: branded share sheet; download is a branded card (lockup, name, caption) around the same code.
- `/users/[id]`: V3 Explore profile (cover, identity card, stats, Follow, Bài đăng / Chia sẻ tabs, 3:4 grid). Own id still redirects to `/profile`.
- `/profile`, `/profile/edit`: cover image on the hero when set; cover upload/remove control on edit.

## 6. Security changes

| Area | Change |
|---|---|
| Storage (owner-applied, verified) | `gs://tappyai-media-prod`: `allUsers` `objectViewer` → `legacyObjectReader` (get only). Anonymous LIST 403, GET 206, PUT/DELETE 403; SA binding unchanged. No code change. |
| `music_tracks` | Lockdown kept; the library is read only through route handlers with the service-role client and a hard `music_type` filter — no client-direct read, `original_sound` rows unreachable by any id. |
| `POST /api/reviews` | Attach is validated server-side against the library (client-supplied ids that are not library rows → 410). |
| Group avatar | Creator-only (explicit 403 + RLS-scoped UPDATE with row check); magic-byte sniff; size cap; `avatars/` prefix; social-write boundary test covers the route. |
| Profile cover | Anonymous refused (B17); sniffed; size cap; `covers/` prefix; owner-only by verified session id + RLS. |
| Share links | QR/profile share hands out only `absoluteUrl('/users/<id>')` (canonical host; `isShareableUrl` refuses anything else). |
| Data hygiene | No new secrets; no new anon grants; `docs/uat/evidence/soundhelix-…html` is a public web page copy. |

## 7. Tests added / changed

Added (10 files): `src/lib/nav/inAppBack.test.ts` (7), `src/app/users/[id]/publicProfile.test.tsx`, `src/app/api/profile/coverAndBio.test.ts`, `src/app/api/reviews/mine/selfScope.test.ts`, `src/app/api/users/[id]/publicFields.test.ts`, `src/app/profile/edit/editProfileCover.test.tsx`, `src/modules/music/**/*.test.*` (6: categoryLabel, musicThumbnail, musicTrackCard, formatDuration, normalizeSearch, validateSelection).
Changed (9): listed in §1 "Tests modified".
Removed: none. (The snapshot's `repository/getTrackById.test.ts` and `getTracksCategoryFilter.test.ts` — written for the old anon-client repository — were not restored.)

## 8. Known pre-existing failures (identical on a pristine `7487b4d` checkout)

| Suite | Cause |
|---|---|
| `src/lib/i18n/clientFeatureParity.test.ts › Music` | Android `app/music/` folder deleted in `ae2a777` (music-reuse removal); the parity table still lists it. |
| `src/components/chatPlaceDecisionWiring.test.tsx › still renders after the turn is saved…` | Unrelated chat card wiring. |
| `supabase/tests/portAllocation.test.ts` | 3 embedded-Postgres suites share a port. |
| `supabase/tests/{cohort_metrics_rollup,daily_snapshots_rollup,moderation_queue_boundary,user_notes_boundary}.test.ts` | Embedded Postgres on this host runs WIN1252: `0xe2 0x9c 0x85 … no equivalent in encoding "WIN1252"`. |

Full run: 13 777 passed / 3 failed / 210 skipped tests (734 / 7 / 11 files). Touched suites: 0 failures beyond the two listed above.

## 9. Known deployment-only checks

1. **Uploads** — composer photo/video, profile avatar, profile cover, group avatar all need Vercel's OIDC token (`VERCEL_OIDC_TOKEN` via `vercel env pull`, or a deploy). Locally every upload answers 500 `upload_failed` honestly.
2. **Migrations on production** — `20260922_groups_avatar_url.sql` and `20260915_profile_public_presentation.sql` must be applied (both additive; the code tolerates their absence). `20260922_music_soundhelix_attribution.sql` only matters where the SoundHelix seed exists.
3. **Music library in production** — the ~100 Jamendo rows are hot-linked under Jamendo's non-commercial API terms with no recorded licence; they WILL be served by `/api/music/tracks` (they are `royalty_free`) until the licensing decision is made. Options: license, self-host CC-BY with attribution recorded, or deactivate. Nothing in this batch changes them.
4. **Bucket IAM** — already applied and verified; re-check after deploy that an authenticated upload (SA `objectUser`) still succeeds.
5. **Explore feed content** — the 4 seeded clips exist only on the audit DB; production has its own.
6. **Android/iOS** — unchanged by this batch. iOS still ships the old music-reuse module (F-024 note); Android has no music surface (parity test).
