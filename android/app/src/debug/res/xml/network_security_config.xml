<?xml version="1.0" encoding="utf-8"?>
<!-- Debug-build-only (src/debug/, never merged into staging/release): allows cleartext HTTP
     to the local dev server (10.0.2.2 = emulator's host-loopback alias; localhost/127.0.0.1
     for a physical device via `adb reverse`). Staging/release use real HTTPS base URLs and
     get no such exception — cleartext traffic stays blocked for them, Android's secure
     default for targetSdk 28+. -->
<network-security-config>
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">localhost</domain>
        <domain includeSubdomains="false">127.0.0.1</domain>
    </domain-config>
</network-security-config>
