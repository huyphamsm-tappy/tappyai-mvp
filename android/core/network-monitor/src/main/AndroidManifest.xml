<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Declared here (not just in :app) so any consumer of this module gets the permission
         merged in automatically — ConnectivityNetworkMonitor doesn't work without it. -->
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
</manifest>
