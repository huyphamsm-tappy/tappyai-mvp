# dependencies
/node_modules
/.pnp
.pnp.js
.yarn/install-state.gz

# testing
/coverage

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# local env files
.env*.local
.env

# vercel
.vercel

# SuperTux large binary assets — now in Vercel Blob, not tracked in git
public/games/supertux/supertux2.data
public/games/supertux/supertux2.wasm
.blob-urls.env

# typescript
*.tsbuildinfo
next-env.d.ts

# Claude IDE settings
.claude/
.env*

# iOS secrets — local only, never commit
ios/Config/Secrets.xcconfig

# Dev helper scripts (root-level only)
/*.bat
/*.ps1
/*.vbs
/*.log
do_push.ps1

# Android local credentials (Supabase URL/anon key, Google Web Client ID) — supplied locally via
# -P overrides or this file; never commit real project credentials, even though the anon key is
# meant to be public in a shipped APK (Production Audit Sprint finding: currently untracked,
# adding this defensively so a future `git add -A` can't accidentally stage it).
android/gradle.properties
